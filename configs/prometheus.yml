# Prometheus 配置示例
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'wolink-core'
    static_configs:
      - targets: ['localhost:8080']
    metrics_path: '/metrics'
    scrape_interval: 5s