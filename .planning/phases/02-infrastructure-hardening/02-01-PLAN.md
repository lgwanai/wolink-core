---
phase: 02-infrastructure-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/validator.go
autonomous: true
requirements:
  - INFRA-01
  - INFRA-04
  - INFRA-05
  - INFRA-06
must_haves:
  truths:
    - "Configuration contains infrastructure settings with sensible defaults"
    - "All timeout and pool values are configurable via YAML or environment variables"
    - "Configuration validates that timeouts are positive durations"
  artifacts:
    - path: "internal/config/config.go"
      provides: "InfrastructureConfig, DBPoolConfig, RedisPoolConfig structs"
      contains: "type InfrastructureConfig struct"
    - path: "internal/config/validator.go"
      provides: "Validation for infrastructure config"
      contains: "validateInfrastructure"
  key_links:
    - from: "setDefaults()"
      to: "infrastructure.*"
      via: "viper.SetDefault"
      pattern: "viper.SetDefault\\(\"infrastructure"
---

<objective>
Add infrastructure configuration structs for graceful shutdown timeout, HTTP server timeouts, and connection pool settings. This provides the configuration foundation that main.go and utility functions will consume.

Purpose: Centralize all infrastructure-related settings with sensible production defaults.
Output: InfrastructureConfig, DBPoolConfig, RedisPoolConfig structs with defaults and validation.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-infrastructure-hardening/02-RESEARCH.md

<interfaces>
<!-- Existing config pattern from internal/config/config.go -->

```go
type Config struct {
    Server   ServerConfig   `mapstructure:"server"`
    Database DatabaseConfig `mapstructure:"database"`
    Redis    RedisConfig    `mapstructure:"redis"`
    Log      LogConfig      `mapstructure:"log"`
    Security SecurityConfig `mapstructure:"security"`
    Models   ModelsConfig   `mapstructure:"models"`
}

func Load() (*Config, error)
func setDefaults()
```

<!-- Existing validator pattern from internal/config/validator.go -->

```go
func (c *Config) Validate() error
func (c *Config) MustValidate()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add infrastructure configuration structs</name>
  <files>internal/config/config.go</files>
  <behavior>
    - Test 1: InfrastructureConfig unmarshals from YAML correctly
    - Test 2: DBPoolConfig has correct default values
    - Test 3: RedisPoolConfig has correct default values
    - Test 4: All duration fields parse correctly (e.g., "30s", "5m")
  </behavior>
  <action>
    Add the following to `internal/config/config.go`:

    1. Add InfrastructureConfig struct with fields:
       - ShutdownTimeout time.Duration `mapstructure:"shutdown_timeout"`
       - ReadTimeout time.Duration `mapstructure:"read_timeout"`
       - WriteTimeout time.Duration `mapstructure:"write_timeout"`
       - IdleTimeout time.Duration `mapstructure:"idle_timeout"`
       - ReadHeaderTimeout time.Duration `mapstructure:"read_header_timeout"`
       - Database DBPoolConfig `mapstructure:"database_pool"`
       - Redis RedisPoolConfig `mapstructure:"redis_pool"`

    2. Add DBPoolConfig struct with fields:
       - MaxOpenConns int `mapstructure:"max_open_conns"`
       - MaxIdleConns int `mapstructure:"max_idle_conns"`
       - ConnMaxLifetime time.Duration `mapstructure:"conn_max_lifetime"`
       - ConnMaxIdleTime time.Duration `mapstructure:"conn_max_idle_time"`

    3. Add RedisPoolConfig struct with fields:
       - PoolSize int `mapstructure:"pool_size"`
       - MinIdleConns int `mapstructure:"min_idle_conns"`
       - ConnMaxLifetime time.Duration `mapstructure:"conn_max_lifetime"`
       - PoolTimeout time.Duration `mapstructure:"pool_timeout"`

    4. Add Infrastructure field to Config struct:
       - Infrastructure InfrastructureConfig `mapstructure:"infrastructure"`

    5. Add defaults in setDefaults():
       - shutdown_timeout: 30s
       - read_timeout: 15s
       - write_timeout: 30s
       - idle_timeout: 120s
       - read_header_timeout: 5s
       - database_pool.max_open_conns: 25
       - database_pool.max_idle_conns: 10
       - database_pool.conn_max_lifetime: 5m
       - redis_pool.pool_size: 20
       - redis_pool.min_idle_conns: 5

    Do NOT add validation in this task - that's Task 2.
  </action>
  <verify>
    <automated>go test -v ./internal/config/... -run TestInfrastructureConfig</automated>
  </verify>
  <done>InfrastructureConfig, DBPoolConfig, RedisPoolConfig structs exist with all fields and defaults set</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add infrastructure configuration validation</name>
  <files>internal/config/validator.go, internal/config/config.go</files>
  <behavior>
    - Test 1: Zero or negative shutdown timeout returns error
    - Test 2: Zero or negative read timeout returns error
    - Test 3: Zero or negative write timeout returns error
    - Test 4: Valid infrastructure config passes validation
  </behavior>
  <action>
    Extend `internal/config/validator.go`:

    1. Add validateInfrastructure() method that checks:
       - ShutdownTimeout > 0
       - ReadTimeout > 0
       - WriteTimeout > 0
       - IdleTimeout > 0
       - ReadHeaderTimeout > 0

    2. Call validateInfrastructure() from Validate() method.

    Error messages should be clear, e.g.:
       - "shutdown timeout must be positive"
       - "read timeout must be positive"

    Note: Connection pool values (MaxOpenConns, etc.) can be zero to mean "use Go/driver defaults", so don't validate them.
  </action>
  <verify>
    <automated>go test -v ./internal/config/... -run TestInfrastructureValidation</automated>
  </verify>
  <done>Infrastructure config validates that all timeout values are positive</done>
</task>

</tasks>

<verification>
- Run `go test -v ./internal/config/...` - all tests pass
- Run `go build ./...` - compiles without errors
</verification>

<success_criteria>
- InfrastructureConfig struct exists with all timeout and pool fields
- DBPoolConfig struct exists with connection pool settings
- RedisPoolConfig struct exists with pool settings
- Defaults are set for all infrastructure values
- Validation rejects zero/negative timeout values
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-hardening/02-01-SUMMARY.md`
</output>
