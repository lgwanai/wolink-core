---
phase: 01-security-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - go.mod
  - internal/config/config_test.go
  - internal/config/validator_test.go
  - internal/api/middleware/cors_test.go
  - internal/models/validation_test.go
  - internal/api/handlers/validation_test.go
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Test infrastructure is in place for all subsequent plans"
    - "All test files exist with failing test stubs"
    - "stretchr/testify is installed and available"
  artifacts:
    - path: "go.mod"
      provides: "testify dependency"
      contains: "github.com/stretchr/testify"
    - path: "internal/config/config_test.go"
      provides: "Config test scaffold"
      min_lines: 10
    - path: "internal/config/validator_test.go"
      provides: "Validator test scaffold"
      min_lines: 10
    - path: "internal/api/middleware/cors_test.go"
      provides: "CORS middleware test scaffold"
      min_lines: 10
    - path: "internal/models/validation_test.go"
      provides: "Models validation test scaffold"
      min_lines: 10
    - path: "internal/api/handlers/validation_test.go"
      provides: "Handlers validation test scaffold"
      min_lines: 10
  key_links: []
---

<objective>
Establish test infrastructure and create test file scaffolds that Wave 1+ plans will populate with actual tests.

Purpose: Satisfy Nyquist compliance by ensuring test files exist before implementation plans run. Each Wave 1+ plan will populate these scaffolds with actual test cases.
Output: testify dependency installed, test file scaffolds created with failing placeholder tests.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install testify assertion library</name>
  <files>go.mod, go.sum</files>
  <action>
    Install the testify assertion library which is used by all subsequent test files:

    ```bash
    go get github.com/stretchr/testify
    ```

    This adds testify as a direct dependency in go.mod.
  </action>
  <verify>
    <automated>grep -q "github.com/stretchr/testify" go.mod && echo "testify installed"</automated>
  </verify>
  <done>
    - testify is listed in go.mod
    - go.sum is updated with testify checksums
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test file scaffolds with failing placeholder tests</name>
  <files>internal/config/config_test.go, internal/config/validator_test.go, internal/api/middleware/cors_test.go, internal/models/validation_test.go, internal/api/handlers/validation_test.go</files>
  <action>
    Create each test file with a placeholder test that fails, signaling that Wave 1+ plans must populate real tests.

    1. internal/config/config_test.go:
       ```go
       package config

       import (
           "testing"

           "github.com/stretchr/testify/assert"
       )

       // TestConfigLoad_Placeholder is a placeholder test.
       // Wave 1 Plan 01 will replace this with actual config loading tests.
       func TestConfigLoad_Placeholder(t *testing.T) {
           t.Skip("Placeholder - Wave 1 Plan 01 will implement real tests")
       }
       ```

    2. internal/config/validator_test.go:
       ```go
       package config

       import (
           "testing"
       )

       // TestConfigValidate_Placeholder is a placeholder test.
       // Wave 1 Plan 01 will replace this with actual validation tests.
       func TestConfigValidate_Placeholder(t *testing.T) {
           t.Skip("Placeholder - Wave 1 Plan 01 will implement real tests")
       }
       ```

    3. internal/api/middleware/cors_test.go:
       ```go
       package middleware

       import (
           "testing"
       )

       // TestCORS_Placeholder is a placeholder test.
       // Wave 1 Plan 02 will replace this with actual CORS tests.
       func TestCORS_Placeholder(t *testing.T) {
           t.Skip("Placeholder - Wave 1 Plan 02 will implement real tests")
       }
       ```

    4. internal/models/validation_test.go:
       ```go
       package models

       import (
           "testing"
       )

       // TestValidation_Placeholder is a placeholder test.
       // Wave 2 Plan 03 will replace this with actual validation tests.
       func TestValidation_Placeholder(t *testing.T) {
           t.Skip("Placeholder - Wave 2 Plan 03 will implement real tests")
       }
       ```

    5. internal/api/handlers/validation_test.go:
       ```go
       package handlers

       import (
           "testing"
       )

       // TestFormatValidationError_Placeholder is a placeholder test.
       // Wave 2 Plan 03 will replace this with actual tests.
       func TestFormatValidationError_Placeholder(t *testing.T) {
           t.Skip("Placeholder - Wave 2 Plan 03 will implement real tests")
       }
       ```

    DO NOT:
    - Do not write actual test logic - Wave 1+ plans own that
    - Do not remove placeholder tests until the owning plan replaces them
  </action>
  <verify>
    <automated>go test -v ./internal/config/... ./internal/api/middleware/... ./internal/models/... ./internal/api/handlers/... 2>&1 | grep -c "SKIP" | xargs -I {} test {} -ge 5 && echo "All placeholder tests exist"</automated>
  </verify>
  <done>
    - All 5 test files exist
    - Each contains a placeholder test that skips
    - Running tests shows 5 skipped tests
  </done>
</task>

</tasks>

<verification>
Run all tests to verify scaffolds exist:
```bash
go test -v ./internal/config/... ./internal/api/middleware/... ./internal/models/... ./internal/api/handlers/...
```

Expected: All placeholder tests show as SKIP.
</verification>

<success_criteria>
- [ ] testify installed as direct dependency
- [ ] internal/config/config_test.go exists with placeholder
- [ ] internal/config/validator_test.go exists with placeholder
- [ ] internal/api/middleware/cors_test.go exists with placeholder
- [ ] internal/models/validation_test.go exists with placeholder
- [ ] internal/api/handlers/validation_test.go exists with placeholder
- [ ] All placeholder tests skip (not fail)
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-foundation/01-00-SUMMARY.md`
</output>
