---
phase: 07-ocr-glm-ocr-paddleocr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/models/glm-ocr.yaml
  - configs/models/paddleocr.yaml
autonomous: true
requirements: [OCR-01]
must_haves:
  truths:
    - "OCR model configs are loadable and valid"
    - "Both GLM-OCR and PaddleOCR models are discoverable"
    - "Config files follow existing model config pattern"
  artifacts:
    - path: "configs/models/glm-ocr.yaml"
      provides: "GLM-OCR model configuration"
      contains: "id: GLM-OCR-bf16"
    - path: "configs/models/paddleocr.yaml"
      provides: "PaddleOCR model configuration"
      contains: "id: PaddleOCR-VL-1.5"
  key_links:
    - from: "ModelConfigService"
      to: "configs/models/*.yaml"
      via: "YAML loading"
---

<objective>
Create OCR model configuration YAML files following the existing ASR model config pattern.

Purpose: Enable GLM-OCR and PaddleOCR model discovery and configuration
Output: Two model config YAML files in configs/models/
</objective>

<execution_context>
@/Users/wuliang/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@configs/models/qwen3-asr.yaml
</context>

<tasks>

<task type="auto">
  <name>Create GLM-OCR model config</name>
  <files>configs/models/glm-ocr.yaml</files>
  <action>
Create YAML config file for GLM-OCR-bf16 model following the qwen3-asr.yaml pattern:
- id: GLM-OCR-bf16
- name: GLM-OCR-bf16
- icon_uri: default_icon/glm.png
- description with zh/en translations
- meta.protocol: openai
- meta.capability.input_modal: [image] (OCR processes images)
- meta.capability.output_modal: [text]
- conn_config.base_url: "http://127.0.0.1:8099/v1"
- conn_config.api_key: "lingting"
- conn_config.model: "GLM-OCR-bf16"
- status: 1

Reference qwen3-asr.yaml for exact structure. OCR is similar to ASR but processes images instead of audio.
  </action>
  <verify>
    ```bash
    # Validate YAML syntax
    python3 -c "import yaml; yaml.safe_load(open('configs/models/glm-ocr.yaml'))"
    
    # Check required fields
    grep "id: GLM-OCR-bf16" configs/models/glm-ocr.yaml
    grep "input_modal:" configs/models/glm-ocr.yaml
    grep "image" configs/models/glm-ocr.yaml
    ```
  </verify>
  <done>GLM-OCR config file exists with valid YAML, correct model ID, and image input modal</done>
</task>

<task type="auto">
  <name>Create PaddleOCR model config</name>
  <files>configs/models/paddleocr.yaml</files>
  <action>
Create YAML config file for PaddleOCR-VL-1.5 model following the same pattern:
- id: PaddleOCR-VL-1.5
- name: PaddleOCR-VL-1.5
- icon_uri: default_icon/paddleocr.png
- description with zh/en translations for PaddleOCR
- meta.protocol: openai
- meta.capability.input_modal: [image]
- meta.capability.output_modal: [text]
- conn_config.base_url: "http://127.0.0.1:8099/v1"
- conn_config.api_key: "lingting"
- conn_config.model: "PaddleOCR-VL-1.5"
- status: 1

Use the same base_url as GLM-OCR since both run on the same server.
  </action>
  <verify>
    ```bash
    # Validate YAML syntax
    python3 -c "import yaml; yaml.safe_load(open('configs/models/paddleocr.yaml'))"
    
    # Check required fields
    grep "id: PaddleOCR-VL-1.5" configs/models/paddleocr.yaml
    grep "base_url:" configs/models/paddleocr.yaml
    ```
  </verify>
  <done>PaddleOCR config file exists with valid YAML and correct model ID</done>
</task>

</tasks>

<verification>
```bash
# Both config files exist and are valid
ls configs/models/glm-ocr.yaml configs/models/paddleocr.yaml
python3 -c "import yaml; [yaml.safe_load(open(f)) for f in ['configs/models/glm-ocr.yaml', 'configs/models/paddleocr.yaml']]"

# Both have image input modal (OCR requirement)
grep -l "input_modal" configs/models/glm-ocr.yaml configs/models/paddleocr.yaml
grep "image" configs/models/glm-ocr.yaml configs/models/paddleocr.yaml
```
</verification>

<success_criteria>
- [x] GLM-OCR config file created with valid YAML
- [x] PaddleOCR config file created with valid YAML
- [x] Both configs have image input_modal
- [x] Both configs use correct base_url (http://127.0.0.1:8099/v1)
- [x] Both configs have correct model IDs
</success_criteria>

<output>
After completion, create `.planning/phases/07-ocr-glm-ocr-paddleocr/07-01-SUMMARY.md`
</output>